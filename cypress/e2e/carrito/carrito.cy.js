// Testear el inicio correcto del Home

describe("Validamos links",() => {
  it("Visitar la pagina", () => {
    cy.visit("https://cypress-playground.vercel.app/");
cy.get('[data-testid="section-card-shadow-iframe"]')
  });
});